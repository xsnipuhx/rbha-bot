// import {  Command } from 'bastion'

// const Ping: Command = ({reply}, config: Config) => reply(config.response)

// const defaultConfig: Config = {
//   response: "Hello World!"
// }

// interface Config {
//   /** The message to respond with when someone pings */
//   response: string;
// }

// export default cmd(Ping, defaultConfig)